export const StatusBadge = ({ value }: { value: string }) => (
  <span className="rounded-full border border-slate-200 bg-slate-50 px-2.5 py-1 text-xs font-semibold tracking-wide text-slate-700">
    {value}
  </span>
);
